{
  "name": "@realtime-labs/grpc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace @realtime-labs/grpc-server && npm run build --workspace @realtime-labs/grpc-client",
    "dev": "npm run dev --workspace @realtime-labs/grpc-server",
    "test": "npm run test --workspace @realtime-labs/grpc-server"
  }
}
